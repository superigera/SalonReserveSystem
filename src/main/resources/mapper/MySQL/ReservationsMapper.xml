<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.repository.ReservationsMapper">

	<!-- 予約登録 -->
	<insert id="reserve">
		insert into reservations(
	 	menu_id,
	 	non_member_nane,
		non_member_email,
		non_member_phone_number,
		reserve_days,
		reserve_time
		)
		values(
		#{menu_id},
		#{non_member_nane},
		#{non_member_email},
		#{non_member_phone_number},
		#{reserve_days},
		#{reserve_time}
		)
	</insert>
	
</mapper>


